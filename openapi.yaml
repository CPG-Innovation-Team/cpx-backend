# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Portal API
    description: The user service definition.
    version: 0.0.1
paths:
    /v1/login:
        post:
            tags:
                - Portal
            description: The user logs in
            operationId: Portal_Login
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LoginReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/logout:
        post:
            tags:
                - Portal
            description: The user logs out
            operationId: Portal_Logout
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LogoutRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LogoutReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/register:
        post:
            tags:
                - Portal
            description: Register user
            operationId: Portal_Register
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RegisterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/user/list:
        post:
            tags:
                - Portal
            description: List of users
            operationId: Portal_GetUserList
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetUserListRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetUserListReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/user/profile/{uid}:
        get:
            tags:
                - Portal
            description: Get Single User Profile
            operationId: Portal_GetUserProfile
            parameters:
                - name: uid
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: int64
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UserProfileReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/user/update:
        put:
            tags:
                - Portal
            description: Update user information
            operationId: Portal_Update
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GetUserListReply:
            type: object
            properties:
                user:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetUserListReply_User'
                type:
                    type: integer
                    format: int64
            description: The response message containing the user list.
        GetUserListReply_User:
            type: object
            properties:
                uid:
                    type: integer
                    format: int64
                name:
                    type: string
                nickname:
                    type: string
                avatar:
                    type: string
                gender:
                    type: integer
                    description: 性别:0-保密;1-男;2-女;
                    format: uint64
                introduce:
                    type: string
        GetUserListRequest:
            type: object
            properties:
                uid:
                    type: array
                    items:
                        type: integer
                        format: int64
                type:
                    type: integer
                    format: int64
            description: The request message of get list user profile.
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        LoginReply:
            type: object
            properties:
                uid:
                    type: integer
                    format: int64
                token:
                    type: string
            description: The response message containing the user token and uid.
        LoginRequest:
            type: object
            properties:
                email:
                    type: string
                password:
                    type: string
            description: The request message containing the user email and password.
        LogoutReply:
            type: object
            properties: {}
            description: The response message is null.
        LogoutRequest:
            type: object
            properties:
                token:
                    type: string
            description: The request message containing the user token.
        RegisterReply:
            type: object
            properties:
                uid:
                    type: integer
                    format: int64
                token:
                    type: string
            description: The response message containing the user token and uid.
        RegisterRequest:
            type: object
            properties:
                user:
                    $ref: '#/components/schemas/RegisterRequest_User'
            description: The request message of register.
        RegisterRequest_User:
            type: object
            properties:
                name:
                    type: string
                nickname:
                    type: string
                type:
                    type: integer
                    description: 类型。1-用户；2-供应商；3-运营
                    format: uint64
                email:
                    type: string
                password:
                    type: string
                avatar:
                    type: string
                gender:
                    type: integer
                    description: 性别:0-保密;1-男;2-女;
                    format: uint64
                introduce:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateReply:
            type: object
            properties:
                uid:
                    type: integer
                    format: int64
                token:
                    type: string
            description: The response message containing the user token.
        UpdateRequest:
            type: object
            properties:
                user:
                    $ref: '#/components/schemas/UpdateRequest_User'
            description: The request message of update user.
        UpdateRequest_User:
            type: object
            properties:
                name:
                    type: string
                nickname:
                    type: string
                type:
                    type: integer
                    description: 类型。1-用户；2-供应商；3-运营
                    format: uint64
                email:
                    type: string
                password:
                    type: string
                avatar:
                    type: string
                gender:
                    type: integer
                    description: 性别:0-保密;1-男;2-女;
                    format: uint64
                introduce:
                    type: string
        UserProfileReply:
            type: object
            properties:
                user:
                    $ref: '#/components/schemas/UserProfileReply_User'
            description: The response message containing the user profile.
        UserProfileReply_User:
            type: object
            properties:
                uid:
                    type: integer
                    format: int64
                name:
                    type: string
                nickname:
                    type: string
                avatar:
                    type: string
                gender:
                    type: integer
                    description: 性别:0-保密;1-男;2-女;
                    format: uint64
                introduce:
                    type: string
tags:
    - name: Portal
